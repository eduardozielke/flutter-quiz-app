import 'package:flutter/material.dart';

class TextStyles {
  static const quizQuestion = TextStyle();

  static const appBarTitle = TextStyle(fontSize: 30);
  static const playAndNext = TextStyle(fontSize: 18);
  static const label1 = TextStyle(color: Colors.white, fontSize: 20);
}
